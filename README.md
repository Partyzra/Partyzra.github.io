# V4.15.3 — Even Film Spacing

Replace only:

- `css/style.css`

This removes the old extra bottom spacing from the Horses clip (`film-motion-intro--right`) and moves that larger end-of-stack spacing to the Deer clip instead.

Result:
- Forest → Horses: normal stack gap
- Horses → Deer: same normal stack gap
- Deer → Film & Video content: larger section break remains

No video sizing, alignment, loading, or playback behavior changed.
